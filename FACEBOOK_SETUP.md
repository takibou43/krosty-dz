# ربط الموقع بصفحة فيسبوك — دليل التفعيل

الكود جاهز ومنشور بالكامل. ينقص فقط **مفتاحان** تضعهما في Vercel، وبعدها كل إعلان جديد
يُنشر تلقائياً على صفحتك.

صفحتك: https://www.facebook.com/profile.php?id=61593807188038

---

## الخطوة 1 — أنشئ تطبيق Meta

1. افتح https://developers.facebook.com/apps ثم **Create App**
2. اختر نوع التطبيق: **Business**
3. اسم التطبيق: `Crosty DZ` — والبريد: بريدك
4. بعد الإنشاء، من القائمة الجانبية: **Add Product** ← أضف **Facebook Login for Business**

---

## الخطوة 2 — اجلب مفتاح الصفحة (Page Access Token)

1. افتح https://developers.facebook.com/tools/explorer
2. أعلى اليمين، اختر تطبيقك `Crosty DZ` من قائمة **Meta App**
3. من قائمة **User or Page** اختر **Get Page Access Token**
4. اختر صفحة `CROSTY` واسمح بالصلاحيات
5. من **Permissions** تأكّد أن هذه الصلاحيات مفعّلة:
   - `pages_manage_posts`  ← النشر على الصفحة
   - `pages_read_engagement`
   - `pages_show_list`
6. اضغط **Generate Access Token** وانسخ المفتاح

> ⚠️ هذا المفتاح قصير الأجل (ساعة واحدة). حوّله إلى **مفتاح دائم** في الخطوة 3.

---

## الخطوة 3 — حوّله إلى مفتاح دائم (مهم جداً)

بدون هذه الخطوة سيتوقف النشر بعد ساعة.

1. افتح https://developers.facebook.com/tools/debug/accesstoken
2. الصق المفتاح واضغط **Debug**
3. اضغط **Extend Access Token** ← ستحصل على مفتاح مستخدم يدوم 60 يوماً
4. ارجع إلى Graph API Explorer، الصق المفتاح الممدَّد، ونفّذ هذا الطلب:

```
GET /me/accounts
```

5. في النتيجة ابحث عن صفحتك، وانسخ قيمة `access_token` الخاصة بها
   — **هذا هو المفتاح الدائم** الذي لا تنتهي صلاحيته طالما لم تُغيّر كلمة مرور فيسبوك.

انسخ أيضاً قيمة `id` الخاصة بالصفحة من نفس النتيجة.

---

## الخطوة 4 — ضع المفتاحين في Vercel

1. افتح https://vercel.com/auto-dz/krosty-dz/settings/environment-variables
2. أضف المتغيّرات التالية (Environment: **Production**):

| الاسم | القيمة |
|---|---|
| `FACEBOOK_PAGE_ID` | `61593807188038` (أو الـ id من الخطوة 3) |
| `FACEBOOK_PAGE_ACCESS_TOKEN` | المفتاح الدائم من الخطوة 3 |
| `NEXT_PUBLIC_SITE_URL` | `https://krosty-dz.vercel.app` |

3. **مهم:** بعد الحفظ، اذهب إلى Deployments ← اضغط `...` على آخر نشر ← **Redeploy**
   (المتغيّرات لا تُطبَّق إلا بعد إعادة نشر)

---

## الخطوة 5 — جرّب

انشر إعلاناً تجريبياً من الموقع. خلال ثوانٍ يجب أن يظهر منشور على صفحتك يحتوي:
الصور، العنوان، السعر، المواصفات، الولاية، ورابط الإعلان.

---

## ملاحظات

**الأمان:** المفتاح يبقى على الخادم فقط ولا يصل إلى متصفح الزوار إطلاقاً.
كما أن الخادم يتحقق من هوية الناشر ويرفض النشر إن لم يكن الإعلان يخصّه —
أي لا يستطيع أحد استغلال الموقع للنشر على صفحتك.

**إن لم تُضبط المفاتيح:** الموقع يعمل طبيعياً وينشر الإعلانات، ويتخطّى فيسبوك بصمت
دون أي رسالة خطأ للمستخدم. وكذلك إن فشل فيسبوك لأي سبب — الإعلان يُنشر على الموقع دائماً.

**نص المنشور:** يُبنى تلقائياً من بيانات الإعلان (مجاناً وبلا أخطاء).
إذا أردت صياغة أذكى بالذكاء الاصطناعي، أضف متغيّراً اختيارياً اسمه `ANTHROPIC_API_KEY`
وسيعيد Claude صياغة المنشور بأسلوب جذّاب — مع الحفاظ على السعر والرابط كما هما،
والرجوع للنص العادي تلقائياً عند أي عطل.
